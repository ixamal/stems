# RUNBOOK — first NUO-STEMS session

You are on `ix`. Traktor Pro 4.5.1 lives here. You are not rendering for another laptop.

## Welcome: Choose Your NUO-STEMS Mode

1. Leave **Traktor Pro 4 Native** selected (Recommended).
2. Hit **Understand**. Not Show Later — lock the mode.
3. Ignore Legacy / Export. Ignore DAW / Non-Traktor.

That’s the whole first decision.

## Then (next chat walks this)

1. Point NUO at the Traktor Pro 4 collection it already sees. If it asks for a path, we log it in `docs/settings/nuo-stems-4/NOTES.md`.
2. Pick **one** track you know. Generate.
3. Open Traktor. Confirm it is a STEM, not a ghost file on disk we forgot to link.
4. Write what happened in `docs/PROGRESS.md`. No audio in git.

## Do not

- Commit `.stem.m4a`, WAV, FLAC, NML, or `master.db`.
- Flip to Legacy “just to see.” That’s a second-computer workflow.
- Open the blackhole repo to debug NUO. Mixer stays over there.
